# Generated by GPT-5.6 - the content is being verified and fixed by the developer step-by-step

# S1TBX / Microwave Toolbox Upgrade Review

## Purpose

This document captures the working model for reviewing ALUs against the legacy S1TBX codebase and the current SNAP Microwave Toolbox. It is intended to support upgrade prioritisation for the restored ALUs fork, with a focus on processing quality rather than cosmetic refactoring or API churn.

In the task wording, references to `s1tbx` should be interpreted in the current SNAP context as the Microwave Toolbox successor repository. The archived `s1tbx` repository remains useful as a provenance and naming reference, but the latest benchmark and upgrade target is `microwave-toolbox`.

## Task Definition

The restored ALUs fork performance will be compared to s1tbx benchmarks that are based on the latest available version. A review of the changes introduced to s1tbx relative to the original version of s1tbx on which ALUs was based is required, with a focus on identifying essential logical changes that affect processing quality and which would need to be addressed in further ALUs fork upgrades. A review of these issues will need to be discussed with JRC.D5 in order to prioritize such upgrades, either in the context of this FBE contract or in follow up activities.

## Provenance Model

ALUs is best treated as a partial native port of selected SNAP/S1TBX processing chains. It contains both operator implementations and a reduced SNAP/S1TBX support layer.

| ALUs component | Legacy S1TBX source | Current Microwave Toolbox source | Review relevance |
|---|---|---|---|
| `sentinel1/s1tbx-commons` | `s1tbx-commons` | `sar-commons` | Sentinel-1 metadata, orbit vectors, SAR geocoding helpers, burst metadata |
| `sentinel1/s1tbx-io` | `s1tbx-io` | `sar-io` | SAFE reader, annotation parsing, raster reading, Sentinel-1 product support |
| `sentinel1/io/orbits` | `s1tbx-io-ephemeris` | `sar-io-ephemeris` | Orbit-file lookup, orbit-state-vector update, POD handling |
| `algs/apply-orbit-file-op` | `s1tbx-io-ephemeris/ApplyOrbitFileOp.java` | `sar-io-ephemeris/ApplyOrbitFileOp.java` | Orbit update and state-vector generation |
| `algs/sentinel1-calibrate` | `s1tbx-op-calibration` | `sar-op-calibration` | Radiometric calibration and LUT handling |
| `algs/thermal-noise-removal` | `s1tbx-op-calibration/Sentinel1RemoveThermalNoiseOp.java` | `sar-op-calibration/Sentinel1RemoveThermalNoiseOp.java` | Thermal noise LUT logic and quality-sensitive pre-calibration step |
| `algs/backgeocoding` | `s1tbx-op-sentinel1/BackGeocodingOp.java` | `sar-op-sentinel1/BackGeocodingOp.java` | SLC coregistration and coherence chain quality |
| `algs/coherence-cuda` | `s1tbx-op-insar/CoherenceOp.java` | `sar-op-insar/CoherenceOp.java` | Coherence calculation, flat-earth phase, windowing, metadata construction |
| `algs/topsar-*` | `s1tbx-op-sentinel1/TOPSAR*.java` | `sar-op-sentinel1/TOPSAR*.java` | Split, deburst, merge, burst metadata handling |
| `algs/range-doppler-terrain-correction` | `s1tbx-op-sar-processing/RangeDopplerGeocodingOp.java` | `sar-op-sar-processing/RangeDopplerGeocodingOp.java` | Terrain correction and geolocation quality |
| `snap-engine` | `snap-engine` | `snap-engine` | Product model, metadata, DEM, geocoding, utility behavior |

## Upstream Rename Model

The active Microwave Toolbox repository preserves the S1TBX git history. Important rename markers in the Microwave Toolbox history are:

| Commit | Meaning |
|---|---|
| `9aca04cc5` | Rename from `s1tbx-*` modules to `sar-*` / Microwave Toolbox structure |
| `450907298` | Move Java packages from `org.esa.s1tbx.*` to `eu.esa.sar.*` |

This allows review work to follow Java file history across the rename boundary instead of treating Microwave Toolbox as an unrelated project.

## Review Objective

The review should not attempt to port every upstream change. The objective is to identify upstream logical changes in Microwave Toolbox, relative to the S1TBX-era logic represented in ALUs, that can affect output quality, supported input coverage, or benchmark comparability.

| Change type | Include in review | Reason |
|---|---|---|
| Numerical algorithm changes | Yes | Can change calibration, coherence, geolocation, or coregistration outputs |
| Metadata interpretation changes | Yes | Can change input parsing, burst boundaries, orbit timing, no-data semantics, band naming |
| Mission/product support changes | Yes | Required for S1C/S1D, updated SAFE formats, GRD/SLC variants, ETAD-related products |
| Default parameter changes | Yes | Can affect benchmark comparability even if algorithms are unchanged |
| Performance-only changes | Case by case | Relevant only if they alter numerical behavior, tile boundaries, caching semantics, or memory precision |
| Package rename/import cleanup | No | Not relevant unless coupled with logic changes |
| UI-only changes | No | Not relevant for ALUs command-line processing unless defaults or graph parameters changed |

## Initial Drift Areas

These quality-affecting areas were identified from local repository inspection and upstream release notes/history. Their current resolution and priority are recorded below.

| Area | Current observation                                                                                                                                                        | ALUs impact hypothesis |
|---|----------------------------------------------------------------------------------------------------------------------------------------------------------------------------|---|
| Sentinel-1C and Sentinel-1D | ALUs supports these by default                                                                                                                                             | Developer review note: ALUs already supports this through generic `SENTINEL-1` mission handling rather than an A/B-only whitelist. Public development notes record an S1D IW SLC coherence run with `alus-coh` using `S1D_IW_SLC__1SDV_20260614T152007_20260614T152033_003234_005A51_C931.SAFE` and `S1D_IW_SLC__1SDV_20260626T152007_20260626T152034_003409_00600E_0BC5.SAFE`; keep this entry under review to confirm whether any remaining reader or metadata drift affects S1C/S1D products |
| Sentinel-1 reader | Current `sar-io` has substantial changes including S1 reader performance, caching, updated product handling, OCN/WV updates, COG/GSLC-related work                         | ALUs native reader may miss updated SAFE metadata behavior or product variants relevant to latest benchmarks |
| Thermal noise removal | Core IW metadata parsing, range-vector selection, and interpolation changes have been reviewed and implemented; later SNAP-4160 policy changes remain a separate follow-up | Selection/interpolation failures from #37/#39 are resolved; manual S1D output review and optional clipping/NaN/noise-band policy parity remain |
| Calibration | Metadata parsing changes are implemented and the numerical behavior has been reviewed against current inputs                                                           | Reviewed and accepted; no additional calibration formula changes are currently required |
| Backgeocoding | Current `BackGeocodingOp` has extensive post-S1TBX changes, including ETAD-related work, reference/secondary terminology, footprint handling, and performance improvements | ALUs coherence chain depends heavily on this logic; this is likely one of the highest-risk drift areas |
| Coherence | Current `CoherenceOp` has multiple improvements and defaults changed across Microwave Toolbox versions                                                                     | ALUs `coherence-cuda` should be compared for window defaults, flat-earth phase subtraction, no-data behavior, metadata output, and band selection |
| Range-Doppler terrain correction | Current `RangeDopplerGeocodingOp` has bug fixes and default DEM changes                                                                                                    | ALUs terrain correction and benchmark comparability may be affected by DEM defaults, geolocation fixes, boundary handling, and no-data-at-sea behavior |
| Apply orbit file | Current orbit handling has later bug fixes including recent orbit race-condition work in the wider toolbox history                                                         | ALUs orbit update should be reviewed for benchmark parity, especially when using SNAP-compatible orbit directories |
| Default DEM | Microwave Toolbox release notes show defaulting to Copernicus DEM                                                                                                          | ALUs docs and workflows historically use SRTM3 and later added Copernicus DEM COG support; benchmark definitions must pin DEM explicitly |
| ETAD | Current Microwave Toolbox includes ETAD product reading and ETAD-assisted Sentinel-1 processing                                                                             | Immediate next priority and standalone topic for IW workflows |

**S1C/S1D code evidence**: ALUs parses the SAFE platform metadata as `familyName + number`, producing mission strings such as `SENTINEL-1D` in `sentinel1/s1tbx-io/sentinel1/sentinel1_level1_directory.cc`. Its shared validation then checks the `SENTINEL-1` prefix in `snap-engine/snap-engine-utilities/engine-utilities/gpf/input_product_validator.cc` and `sentinel1/s1tbx-commons/sentinel1_utils.cc`. Current Microwave Toolbox has the same generic `Sentinel1Utils` prefix check, while `sar-op-calibration/.../Sentinel1Calibrator.java` also lists `SENTINEL-1A`, `SENTINEL-1B`, `SENTINEL-1C`, and `SENTINEL-1D` explicitly.

## Sentinel-1 Reader Sync Status

The Sentinel-1 reader section is not fully implemented or fully synced with current Microwave Toolbox. The current ALUs changes are a targeted, IW-relevant subset of SAFE Level-1 reader hardening and geocoding fixes. They should be treated as partial sync work, not as completion of the full `sar-io` reader review.

| Upstream commit | Synced ALUs scope | Status |
|---|---|---|
| `12db283e1` `SNAP-3750 ensure case for CDSE product` | Lower-case image names for image-to-band metadata lookup | Synced |
| `d8cd6bb39` `refactor SafeManifest` | Manifest element lookup accepts both `metadataWrap/xmlData` and direct `xmlData` | Synced subset |
| `73e10e98b` `S1TBX-868 new Spacety format` | Optional `orbitReference`, `OrbitNumber`, `relativeOrbitNumber`, and pass fallback nodes are guarded | Synced subset |
| `d8353246c` `[S1TBX-647] Modified S-1 reader to handle image crossing anti-meridian` | Negative longitudes are normalized before tie-point interpolation when the geolocation grid crosses the antimeridian | Synced; further AOI/antimeridian validation is outside the current input scope |
| `a8dd51abe` `SNAP-4185 use ProductCache for netcdf readers` | Only the invalid raster/grid dimension guard before tie-point-grid creation was ported | Partial; ProductCache behavior and annotation-only dimension fallback were not ported |
| `f08897475` `fix prefix` | IW-relevant subset: band geocoding is keyed by TOPSAR prefix (`IW1_`, `IW2_`, `IW3_`) instead of concrete band pointer | Partial; WV-specific behavior from surrounding upstream commits is out of scope |
| `4adf88da0` `handle RCM sim RV RH products` | `ImageIOFile::Close()` closes the underlying reader instead of throwing | Synced behavior |
| `057211babf` `S1TBX-758 use min of product dimensions or 10 for the TPG grid`, `904e622ac0` `avoid 1 pixel grid`, `117bd71c68` `S1TBX-895 subsampling as double` | `ReaderUtils::AddGeoCoding()` clamps generated tie-point-grid size and computes subsampling as double; ALUs also fixes the C++ port-specific `CreateFineTiePointGrid()` output-vector pass-by-value bug | Synced; relevant because Range-Doppler can consume product latitude/longitude tie-point grids |

Annotation-only products are not an ALUs target because ALUs is intended to accelerate computations on products with measurement data. The upstream annotation-only dimension fallback associated with `a8dd51abe` is explicitly not implemented. Remaining Sentinel-1 reader review is limited to IW-relevant `sar-io` behavior, cache/performance changes, SAFE folder/zip qualification drift, RFI metadata, and range-window metadata. The scope remains IW-only, with ETAD tracked separately as the immediate next workflow topic.

The STEP forum report [Geolocation inaccuracy](https://forum.step.esa.int/t/geolocation-inaccuracy/37504) concerns Range-Doppler terrain correction output shifts that vary with TOPSAR subset size. It is not attributed here to `ReaderUtils::CreateFineTiePointGrid()` alone. However, ALUs `ReaderUtils::CreateFineTiePointGrid()` previously took its output vector by value, making `ReaderUtils::AddGeoCoding()` generate zero-filled product latitude/longitude tie-point grids. Since ALUs Range-Doppler terrain correction can consume product latitude/longitude tie-point grids, this helper is retained in the sync queue and fixed rather than discarded.

## Rolling Sync Status

| Order | Sync item | Upstream reference | Current status |
|---|---|---|---|
| 1 | Harden SAFE manifest parsing | `d8cd6bb39`, `73e10e98b` | Submitted in ALUs `c8112e0b`; localized parser guards support wrapped/direct `xmlData` and optional orbit metadata |
| 2 | Add annotation/dimension robustness | `a8dd51abe` | Partial by design: invalid TPG dimension guard submitted; annotation-only dimension fallback explicitly not implemented because measurement-free products are outside ALUs scope |
| 3 | Fix IW tie-point/geocoding robustness | `d8353246c`, `f08897475`, SNAP Engine `057211babf`, `904e622ac0`, `117bd71c68` | Submitted across ALUs `c8112e0b` and `972ed881`; includes antimeridian handling, IW prefix geocoding, fine-grid output-vector fix, grid-size guards, and double subsampling |
| 4 | Improve noise/calibration LUT parsing tolerance | `860a618515`, `e048ff875` | Submitted in ALUs `a98cb7af`: accepts `noiseLut`/`noiseRangeLut`, parses general ASCII whitespace, and validates declared and aligned LUT counts without changing TNR/calibration formulas or range-vector selection |
| 5 | Align thermal-noise vector selection and interpolation | `8be3a2b94`, `38418ee3d`, `7be4ed8a4` | Implemented and verified locally: GRD uses swath-constrained nearest-vector fallback and azimuth-time interpolation; TOPS SLC uses original subset coordinates and `adsHeader.startTime` burst alignment; one-value LUTs are constant and sparse metadata emits warnings. The products from ALUs issues [#37](https://github.com/cgi-estonia-space/ALUs/issues/37) and [#39](https://github.com/cgi-estonia-space/ALUs/issues/39) are covered by the same fallback behavior. The supplied 2023-07-02 GRD failure completes, Virumaa remains pixel-identical to its golden, and the S1D result from `build-automation/estonia-2026-S1D-calibration.sh` has been manually verified and accepted |
| 6 | Review calibration numerical behavior | Current `Sentinel1Calibrator` behavior | Reviewed and manually accepted for current IW inputs; parser changes from `a98cb7af` are retained and no additional formula changes are required |
| 7 | ETAD-assisted IW processing | Current Microwave Toolbox ETAD reader/processing behavior | Immediate next work item and standalone IW topic |
| 8 | Burst-based IW product processing | Current TOPSAR split/deburst/merge and burst metadata behavior | Explicitly follows ETAD |
| 9 | UTC MJD rounding | `ddf5a79cfc` | Pending as a narrow utility fix with a focused regression test; no broad time/datamodel rewrite |

## Recent Session Outcomes

| Topic | Status | Evidence |
|---|---|---|
| SAFE manifest and optional orbit metadata hardening | Implemented and submitted | ALUs `c8112e0b`; wrapped/direct `xmlData` support and guarded optional orbit nodes |
| IW reader tie-point and geocoding robustness | Implemented and submitted | ALUs `c8112e0b`, `972ed881`; antimeridian handling, TOPSAR-prefix geocoding, generated-grid guards, double subsampling, fine-grid output fix, and reader close behavior |
| Noise and calibration metadata parsing | Implemented and submitted | ALUs `a98cb7af`; legacy/current LUT names, general whitespace, declared-count validation, and dataset-independent tests |
| IW thermal-noise value selection and interpolation | Implemented locally and verified | Swath-constrained GRD fallback, azimuth-time interpolation, SLC absolute burst alignment, subset-coordinate handling, one-value LUT support, contextual warnings, 26 thermal-noise tests, and successful reported-failure/partial-split runs |
| Calibration numerical behavior | Reviewed and accepted | Current IW calibration formulas and LUT behavior are considered good; parsing compatibility remains documented in `a98cb7af` |
| Calibration-chain verification | Completed and manually approved | Virumaa is pixel-identical to its golden; `build-automation/estonia-2026-S1D-calibration.sh` covers S1D and its changed output has been manually verified and accepted |

## Lingering Topics

| Area | Status | Remaining action |
|---|---|---|
| ETAD-assisted IW processing | Next, highest priority | Review and implement the Microwave Toolbox ETAD reader and ETAD-assisted processing behavior needed by IW workflows |
| Burst-based IW product processing | Second priority | Follow ETAD with valid-pixel boundaries, timing, subset metadata, split/deburst/merge behavior, and other burst-processing alignment |
| Later SNAP-4160 thermal-noise policies | Lower priority; no current regression | Decide whether ALUs needs configurable negative clipping, explicit zero-noise/noise-band semantics, and broader NaN handling from `646119402`/`4b1f34f17` |
| Sentinel-1 SAFE reader parity | Lower priority; current IW inputs are good | Limit any follow-up to benchmark-relevant IW `sar-io`, SAFE folder/zip, cache, RFI, and range-window behavior |
| Orbit application and POD handling | Lower priority; no current regression | Review orbit-file selection, interpolation, metadata updates, timing, and current race-condition fixes when evidence requires it |
| Range-Doppler terrain correction | Lower priority; current products are good | The fine-grid helper path is fixed; defer broader geolocation, DEM/default, boundary, mask, and sea no-data comparison until a regression appears |
| Backgeocoding and coregistration | Lower priority; current products are good | Retain footprint/bounds, DEM masking, burst pairing, and interpolation as follow-up review areas without treating them as immediate blockers |
| Coherence | Lower priority; current products are good | Retain window defaults, flat-earth phase, no-data, band selection, and metadata output as follow-up review areas without a known regression |
| UTC MJD rounding | Pending narrow fix | Port `ddf5a79cfc` with a focused boundary regression test |
| Benchmark and JRC.D5 decision matrix | Final step | After implementation reviews, pin Microwave Toolbox commit, IW products, DEMs, orbit inputs, defaults, and metrics |

## Stepwise Review Plan

| Step | Output |
|---|---|
| 1. Reconstruct per-component lineage | Identify the closest S1TBX-era source state for each ALUs operator/helper using code comments, ALUs git history, function names, behavior, and available SNAP comparison docs |
| 2. Build upstream change inventory | For each operator/helper, list relevant upstream changes from the reconstructed S1TBX-era lineage to current Microwave Toolbox HEAD, excluding pure rename/package changes |
| 3. Classify logical changes | Mark each change as quality-affecting, input-support-affecting, benchmark-default-affecting, performance-only, or not relevant |
| 4. Map to ALUs code | Connect each relevant upstream change to specific ALUs files and functions |
| 5. Define conformance tests | Select minimal S1 datasets and graph parameters to compare latest S1TBX/Microwave Toolbox outputs against ALUs outputs |
| 6. Prepare JRC.D5 review list | Produce a short priority table with effort, risk, expected quality impact, and contract/follow-up recommendation |

## Baseline Reconstruction

The ALUs upstream baseline should be reconstructed per component, not pinned globally. Useful clues already present in the repository are:

| Clue | Value |
|---|---|
| Existing comparison doc | `docs/SNAP_COMPARISON.md` references SNAP engine `8.0.8` and S1TBX `8.0.5` for ALUs `0.6` |
| Later routine docs | Calibration/coherence READMEs compare against SNAP 8 GPT behavior |
| ALUs SAR history | Most core SAR work is from 2020-2023, with 2026 commits mostly build/toolchain refresh |
| Source comments | Many C++ files explicitly name original Java classes and old `senbox-org/s1tbx` paths |

The continuing review should maintain a per-component lineage table. The lineage does not need to identify one exact upstream commit for every file, but it should identify the closest practical S1TBX-era behavior represented by ALUs before comparing each remaining component against current Microwave Toolbox logic.

## Benchmark Comparability Notes

Benchmark definitions must pin inputs and processing parameters tightly. Otherwise differences may reflect SNAP default changes rather than ALUs logic drift.

| Parameter | Recommendation |
|---|---|
| SNAP/Microwave Toolbox version | Record exact git commit, not only release label |
| DEM source | Pin SRTM3 or Copernicus DEM explicitly |
| Orbit handling | Pin orbit files or orbit directory contents |
| Input product | Record SAFE product name, IPF version, mission, acquisition mode, product type, polarization, and burst/subswath selection |
| No-data handling | Record output no-data value, valid-pixel mask handling, and statistics exclusion rules |
| Intermediate products | Enable ALUs `--wif` and equivalent SNAP graph outputs for debugging when comparing chains |
| Metrics | Compare geotransform, raster size, no-data mask, valid percentage, min/max/mean/stddev, pixel-wise absolute difference, relative difference, and selected visual diagnostics |

## Candidate JRC.D5 Prioritisation Format

| Priority | Issue | Upstream source | ALUs location | Quality risk | Effort | Recommendation |
|---|---|---|---|---|---|---|
| Next | ETAD-assisted IW processing | Microwave Toolbox ETAD reader and Sentinel-1 processing paths | `sentinel1`, burst-processing and geocoding components | High until reviewed | High | Immediate next implementation/review topic |
| Second | Burst-based IW product processing | `TOPSARSplitOp.java`, `TOPSARDeburstOp.java`, `TOPSARMergeOp.java`, burst metadata helpers | `algs/topsar-*` and related burst consumers | Medium | High | Start after ETAD |
| Complete | Thermal noise range-vector selection changes | `sar-op-calibration/Sentinel1RemoveThermalNoiseOp.java` | `algs/thermal-noise-removal` | Resolved for current IW inputs | Medium | Keep issue datasets and calibration scripts as regressions; S1D is manually approved |
| Lower | Backgeocoding post-S1TBX changes | `sar-op-sentinel1/BackGeocodingOp.java` | `algs/backgeocoding` | No current regression | TBD | Retain for later review after the two priority topics |
| Lower | Coherence operator changes and defaults | `sar-op-insar/CoherenceOp.java` | `algs/coherence-cuda` | No current regression | TBD | Retain for later review with benchmark windows and flat-earth settings pinned |
| Lower | Range-Doppler geocoding changes | `sar-op-sar-processing/RangeDopplerGeocodingOp.java` | `algs/range-doppler-terrain-correction` | No current regression | TBD | Retain for later review; current products produce good results |

## Open Questions

| Question | Why it matters |
|---|---|
| What S1TBX-era behavior does each ALUs operator most closely represent? | Required to avoid reviewing changes ALUs already contains |
| What exact Microwave Toolbox version will be used for latest benchmarks? | Required for reproducible comparisons |
| Which IW SLC/GRD, mission, and IPF-version inputs will JRC.D5 use for final benchmarks? | Defines the final IW-only validation matrix |
| What acceptance metrics and tolerances will JRC.D5 use? | Needed only at the final benchmark/decision stage after ETAD and burst-processing work |
